name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  lint-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Install uv
        uses: astral-sh/setup-uv@v8

      - name: Set up Python
        run: uv python install 3.12

      - name: Install dependencies
        run: uv sync --locked --dev

      - name: Run Ruff
        run: uv run ruff check src/ tests/ monitoring/

      - name: Run tests
        # run: uv run pytest tests/ -v
        run: uv run pytest tests/test_preprocess.py -v

  # build-docker:
  #   runs-on: ubuntu-latest
  #   needs: lint-and-test
  #   if: github.event_name == 'push' && github.ref == 'refs/heads/main'

  #   steps:
  #     - name: Check out repository
  #       uses: actions/checkout@v6

  #     - name: Set up Docker Buildx
  #       uses: docker/setup-buildx-action@v3

  #     - name: Build Docker image
  #       run: docker build -t nyc-taxi-api:${{ github.sha }} -f docker/Dockerfile .

  #     - name: Verify API container starts
  #       run: |
  #         docker run -d --name test-api -p 8000:8000 nyc-taxi-api:${{ github.sha }}
  #         sleep 10
  #         curl -f http://localhost:8000/health
  #         docker stop test-api